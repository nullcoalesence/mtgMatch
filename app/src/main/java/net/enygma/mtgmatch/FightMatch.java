package net.enygma.mtgmatch;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;

public class FightMatch extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_fight_match);
    }

    //Save the incremental updates to the 'match' as we go

    //Save the final data on the match, and we should update all Player's history?

}
